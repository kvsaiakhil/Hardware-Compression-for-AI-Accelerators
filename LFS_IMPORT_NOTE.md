This repository was imported from a downloaded GitHub ZIP archive.

Files that were originally stored in Git LFS were not included in the archive as real file contents. GitHub ZIP downloads provide LFS pointer text instead of the underlying objects.

During migration to the personal repository, those pointer files were converted into explicit placeholders so the repository structure could be pushed successfully.

The affected file list used during import was saved locally at:

`/tmp/atalanta_lfs_backup/lfs_pointer_files.txt`
